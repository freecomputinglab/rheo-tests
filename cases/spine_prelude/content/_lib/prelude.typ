// Prepended inside every vertebra, after its own rheo-context() binding — so
// this reads the importing page's handle, not one fixed at parse time.
#let page-handle = rheo-context().handle
#let shout(body) = [!! #body !!]
