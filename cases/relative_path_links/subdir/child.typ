= Child Document

This document is in a subdirectory.

== Link to Parent Directory

Go back to #link("../root.typ")[the root document].

== Link to Sibling (Explicit Same Dir)

See #link("./sibling.typ")[the sibling] in the same directory.

== Link to Sibling (Implicit Same Dir)

Also see #link("sibling.typ")[the sibling again] with implicit path.
