= Child Document

This document is in a subdirectory.

== Link to Parent Directory

Go back to #link(<root>)[the root document].

== Link to Sibling (Explicit Same Dir)

See #link(<sibling>)[the sibling] in the same directory.

== Link to Sibling (Implicit Same Dir)

Also see #link(<sibling>)[the sibling again] with implicit path.
