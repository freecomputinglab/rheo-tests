#set document(title: [A])

= A

`guide/` has no `index.typ` or `guide.typ` landing page, so it becomes a
non-clickable group node titled "Guide" nesting this file as `guide:a`.

Handle: #rheo-context().handle
