= Introduction

This document has no hand-authored labels; every cross-reference relies on a
handle rheo synthesizes from the file path.

See #link(<chapter>)[the chapter] and the #link(<appendix:notes>)[appendix notes].
