#set document(title: [Setup])

= Setup

`01-basics/` has no `index.typ` or `01-basics.typ`, so it becomes a
non-clickable group node titled "Basics" (numeric prefix stripped and
title-cased for display, but kept in the handle/URL below).

Handle: #rheo-context.handle
