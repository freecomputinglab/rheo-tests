#set document(title: [X])

= X

Nested two levels under `guide/`. `guide/deep/` has no `index.typ` or
`deep.typ` landing file, so it becomes a non-clickable group node, and `x.typ`
is its only child.

Handle: #rheo-context.handle
