#set document(title: [Guide])

= Guide

The landing page for the `guide/` directory. Its handle is the directory's
own handle (`guide`), not `guide:index` --- `guide/index.typ` is not a
separate child node.

Handle: #rheo-context().handle
