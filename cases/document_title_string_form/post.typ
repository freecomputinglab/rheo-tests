#set document(title: "My Post")

= My Post

Intro paragraph mentioning #link("https://example.com")[RiffRaff] early in the
body — the historical trigger that made the text-scan grab the link text as the
title.
