// Control page: no <rheo-head> element at all, to prove pages without the
// feature are completely unaffected by the hoist pass. Its reference is
// left byte-identical to today's un-hand-edited output.

= Page C

Just an ordinary paragraph with no head hoist elements.
