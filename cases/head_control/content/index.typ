// RED case for the not-yet-implemented `.rheo/` control-asset prefix (rheo
// bead rheo-head-control-cbr). `content/.marrow.typ` mints `.rheo/head.html`,
// a control asset that rheo should consume internally and append into every
// page's `<head>` rather than write verbatim to the build output. Today rheo
// does neither: `.rheo/head.html` is written as an ordinary asset, and
// nothing is appended to any page's `<head>`.

= Root Page

Root-level vertebra for the head-control fixture.
