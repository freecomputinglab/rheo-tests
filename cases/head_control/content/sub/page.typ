// Nested vertebra: exercises depth-relative correctness of the eventual
// head-control injection pass (see crates/core/src/util/html.rs
// `depth_prefix`, which has historically been the source of injection bugs
// for nested pages).

= Nested Page

A page nested one directory below the content root.
