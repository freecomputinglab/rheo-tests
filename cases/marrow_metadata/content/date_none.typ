#set document(title: [Date None], date: none)

= Date None

Vertebra with `date: none` set explicitly -- case 2 of rheo-tests-date-matrix-vjw,
paired with date_auto.typ: `date: none` and `date: auto` (unset) must both
resolve to "no date" and be indistinguishable in the resolved metadata (no
`date` key present in either).
