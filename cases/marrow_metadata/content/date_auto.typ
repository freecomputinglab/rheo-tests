#set document(title: [Date Auto], date: auto)

= Date Auto

Vertebra with `date: auto` set explicitly (the same value as leaving `date`
unset entirely) -- case 2 of rheo-tests-date-matrix-vjw, paired with
date_none.typ: both must resolve to "no date" identically.
